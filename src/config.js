'use strict';

const path = require( 'path' );

const config = {};

config.repository = path.resolve( __dirname, '../data/' );
// config.repository = 'https://notwikilambda.toolforge.org/wiki/ZObject:';

module.exports = config;
