{
  "name": "function-evaluator",
  "version": "0.0.1",
  "description": "Evaluting function objects",
  "main": "src/function-evaluator.js",
  "scripts": {
    "test": "npm -s run lint && npm -s run unit",
    "unit": "qunit",
    "lint": "eslint --cache --max-warnings 0 ."
  },
  "author": "WikiLambda team",
  "repository": "https://github.com/vrandezo/function-evaluator",
  "license": "MIT",
  "devDependencies": {
    "eslint": "7.10.0",
    "eslint-config-wikimedia": "0.17.0",
    "qunit": "^2.11.3"
  }
}
